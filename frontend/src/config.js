/**
 * Configuration file for API endpoints
 *
 * Production (Render.com):
 * - Uses cloud backend with HTTPS (works on both PC and AR headset)
 *
 * Local development:
 * - Uncomment the localhost line below for local testing
 */

// Production: Cloud backend on Render.com (HTTPS - works with WebXR)
export const API_BASE_URL = 'https://bpmn-ar-backend.onrender.com/api';

// For local development, uncomment this:
// export const API_BASE_URL = 'http://localhost:3000/api';
